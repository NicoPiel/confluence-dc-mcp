"""Async HTTP client for a local OpenAI-compatible embedding inference endpoint."""

# TODO: implement EmbeddingClient with httpx.AsyncClient
# TODO: embed(texts) → list[list[float]]  — auto-batch if len > batch_size
# TODO: embed_single(text) → list[float]
